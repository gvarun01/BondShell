#ifndef PROMPT_UTILS_H
#define PROMPT_UTILS_H

#include "global.h"

void intro();
void display_prompt(char *prompt_message_suffix); // Renamed from 'promt' to 'display_prompt' for clarity and to avoid conflict if 'promt.h' still exists

#endif
